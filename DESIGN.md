# DESIGN.md

## 产品定位
波场 TRC20-USDT 地址风险查询工具。用户在转账前输入对方地址，工具核验「Tether 官方冻结名单 + 链上画像 + 关联地址涉黑情况」，输出可解释的风险等级。属于安全风控 / 合规决策类产品。

## 气质与意象
- 关键词：冷静、克制、精密、值得托付。像深夜里的风控指挥中心 / 司法鉴定实验室，而不是加密赌场。
- 具象场景：深色哑光终端屏幕上，一条地址被逐行解析，风险项像安检机一样被打标——红色代表拦截、琥珀代表警惕、绿色代表放行。信息密度高但呼吸感充足，关键结论一目了然。

## 视觉策略
- 深色为主的「安全终端」界面，近黑墨色背景 + 细网格/微光边框，营造专业监控感。
- 唯一的视觉焦点是**风险结论**：用大面积的等级色块（红/琥珀/绿）锚定用户视线，其余信息退居中性灰。
- 图形语言：等宽字体呈现地址与哈希、细分隔线、状态徽标、风险评分环形仪表。

## 配色方案
- 背景（base）：近黑墨色 `#0A0B0F`，卡片 `#12141B`，边框微光 `#1F2430`。
- 前景主文字 `#E6E9F0`，次要文字 `#8B93A7`。
- 风险语义色：
  - 高危（拦截）：警戒红 `#F4494E`，背景浸染 `rgba(244,73,78,0.12)`。
  - 中险（警惕）：琥珀 `#F5A623`，浸染 `rgba(245,166,35,0.12)`。
  - 低险（放行）：安全绿 `#2ECC8F`，浸染 `rgba(46,204,143,0.12)`。
- 品牌点缀色：冷青 `#38BDF8`，仅用于链接、聚焦态与中性强调，不与风险色争抢注意力。

## 字体排版
- 地址、哈希、数值：等宽字体族（`ui-monospace, "JetBrains Mono", SFMono-Regular, Menlo, monospace`）。
- 正文与标题：系统无衬线（`-apple-system, "PingFang SC", "Microsoft YaHei", sans-serif`）。
- 排版节奏：风险等级用超大字号数字/标签做视觉锚点；检查项采用「左标签 + 右状态」的清单式行布局，紧凑对齐。

## 动效与交互
- 性格：干脆、精确，无弹跳、无冗余炫技。
- 查询时：分步检查清单逐项「扫描」点亮（skeleton/进度），模拟安检过程，建立可信感。
- 风险结果：等级色块与评分环用一次 400~600ms 的 ease-out 淡入 + 轻微上移；数字 0→目标值滚动。
- 悬停/聚焦：仅 1px 冷青描边或微亮背景，过渡 150ms。

## 组件与布局
- 单栏居中查询页：顶部品牌 + 一句价值说明 → 居中大号地址输入框（带 TRON 标识与粘贴按钮）→ 结果卡片。
- 结果卡片区：①风险等级与评分环总览 ②检查项明细列表（命中可展开解释）③链上画像（余额/交易笔数/首末次交易/USDT 流向）④关联风险地址清单 ⑤免责声明。
- 使用 shadcn/ui 组件（Card / Badge / Input / Button / Progress / Accordion / Separator / Skeleton）。

## 可访问性与文案
- 风险不能仅靠颜色区分，必须同时有文字标签（高危/中险/低险）与图标。
- 全程使用半角标点与等宽地址；地址提供复制与在 TronScan 打开的外链。
- 显著且不可关闭地呈现免责声明：结果仅供参考、数据滞后、不构成法律或合规结论。

## 设计禁忌
- 不要蓝紫渐变、霓虹赛博、粒子特效、emoji 图标堆砌。
- 不要把工具包装成「保证安全 / 100% 不冻结」的承诺式营销。
- 不要用浅色糖果风；不要让非风险信息用高饱和色抢焦点。
- 不要在页面渲染期使用随机数或客户端时间导致 hydration 问题。
